<!DOCTYPE html>
<html lang="hu">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Szabadság igénylés beadandó</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <h1>Szabadság igénylés</h1>

                <form action="/leave-request" method="post">
                    <div class="form-group">
                        <label for="name">Név</label>
                        <input type="text" class="form-control" id="name" name="name" value="<%= leaveData.name || '' %>" required>
                        <small class="form-text text-muted">kötelező*</small>
                    </div>

                    <div class="form-group">
                        <label for="sDate">Kezdő Dátum</label>
                        <input type="date" class="form-control" id="sDate" name="sDate" value="<%= leaveData.sDate || '' %>" required>
                        <small class="form-text text-muted">kötelező, a mai napnál nem lehet régebbi*</small>
                    </div>

                    <div class="form-group">
                        <label for="eDate" class="form-label">Vég Dátum</label>
                        <input type="date" class="form-control" id="eDate" name="eDate" value="<%= leaveData.eDate || '' %>" required>
                        <small class="form-text text-muted">kötelező, a kezdő dátumnál későbbi kell legyen*</small>
                    </div>

                    <div class="form-check">
                        <input type="checkbox" class="form-check-input" id="paidLeave" name="paidLeave" <%=leaveData.paidLeave ? 'checked' : '' %>>
                        <label class="form-check-label" for="paidLeave">Fizetett szabadság</label>
                    </div>

                    <div class="form-group">
                        <label for="employeeId" class="form-label">Alkalmazott Azonosító</label>
                        <input type="text" class="form-control" id="employeeId" name="employeeId" placeholder="például: ABC-123" pattern="[A-Z]{3}-[1-9]{3}" value="<%= leaveData.employeeId || '' %>" required>
                        <small class="form-text text-muted">kötelező, BBB-SSS formátumban (B = nagybetű, S = számjegy 1-9)*</small>
                    </div>

                    <div class="form-group">
                        <label for="reason" class="form-label">Indoklás</label>
                        <textarea class="form-control" id="reason" name="reason" rows="4" minlength="30" required> <%= leaveData.reason || '' %></textarea>
                        <small class="form-text text-muted">kötelező, minimum 30 karakter*</small>
                    </div>

                    <button type="submit" class="btn btn-primary">Beküldés</button>
                </form>

                <br>

                <% if (errors && errors.length> 0) { %>
                    <div class="alert alert-danger">
                        <ul>
                            <% errors.forEach(function(error) { %>
                                <li>
                                    <%= error.message %>
                                </li>
                                <% }) %>
                        </ul>
                    </div>
                    <% } %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>